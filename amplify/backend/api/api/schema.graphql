type StudentUser @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  score: Int!
  teacherName: String!
}

type StudentHistory @model @auth(rules: [{ allow: public }]) {
  id: ID!
  level: Int!
  accuracy: Int!
  date: String!
  username: String!
}
